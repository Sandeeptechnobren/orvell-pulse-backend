# Deployment Log

Append one row per deployment (written by `/deploy`) and per rollback (written by `/rollback`).
Keep the **pre-deploy commit hash** so `/rollback` can restore the last known good state.

| Date (UTC) | Action | Branch | Commit | Pre-deploy commit | Server | Result | Notes |
|------------|--------|--------|--------|-------------------|--------|--------|-------|
| _none yet_ |        |        |        |                   |        |        |       |
